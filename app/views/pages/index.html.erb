<h1>Listing pages</h1>

<table>
  <tr>
    <th>Url</th>
    <th>Foo count</th>
    <th>Baz count</th>
    <th></th>
    <th></th>
    <th></th>
  </tr>

<% @pages.each do |page| %>
  <tr>
    <td><%= page.url %></td>
    <td><%= page.foo_count %></td>
    <td><%= page.baz_count %></td>
    <td><%= link_to 'Show', page %></td>
    <td><%= link_to 'Edit', edit_page_path(page) %></td>
    <td><%= link_to 'Destroy', page, method: :delete, data: { confirm: 'Are you sure?' } %></td>
  </tr>
<% end %>
</table>

<br />

<%= link_to 'New Page', new_page_path %>
